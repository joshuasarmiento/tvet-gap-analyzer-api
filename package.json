{
  "name": "tvet-backend",
  "scripts": {
    "dev": "bun --hot api/index.ts",
    "start": "bun api/index.ts",
    "build": "bun build api/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "hono": "latest",
    "xlsx": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}