{
  "name": "tvet-backend",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "build": "bun build index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "hono": "latest",
    "xlsx": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  }
}